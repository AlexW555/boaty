<%# Navigation %>
<nav class="font-sans flex flex-col text-center sm:flex-row sm:text-left sm:justify-between py-4 px-6 bg-white shadow sm:items-baseline w-full">
  <div class="mb-2 sm:mb-0">
    <a href="#" class="text-2xl font-bold no-underline text-grey-darkest hover:text-blue-dark">Boaty 🚣</a>
  </div>
  <div>
    <% if user_signed_in? %> 
      <div class="grid grid-cols-2 gap-3"> Welcome <%= current_user.email %> 
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, form_class: "contents", class: "flex-1" %> 
      </div> 
    <% else %>
      <%= button_to "Sign in", new_user_session_path, class:"font-bold py-2 px-2 bg-blue-400 rounded-md text-white" %> 
    <% end %>
  </div>
</nav>

<h1>Hello This is a page!</h1>
<h1 class="font-bold text-4xl text-green-500">
      Hello world!
</h1>
</div>